[package]
name = "zed_sirin"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone workspace: this crate builds for wasm32-wasip1, not the Sirin workspace.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.6.0"
