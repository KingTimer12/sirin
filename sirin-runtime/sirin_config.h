/* Build configuration: which runtime parts a program uses.
 *
 * The compiler writes the real version of this file next to the runtime for
 * every build, with lines such as `#define SIRIN_USE_VEC_INT` and
 * `#define SIRIN_USE_ASYNC`. This empty default only keeps editors and
 * language servers happy when browsing the runtime sources. */
#ifndef SIRIN_CONFIG_H
#define SIRIN_CONFIG_H
#endif
