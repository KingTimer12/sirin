// Shared HTTP types. Anonymous struct aliases (structural).

type Request = {
    method:  str,
    path:    str,
    headers: Map[str, str],
    body:    str
}

type Response = {
    status:  int,
    headers: Map[str, str],
    body:    str
}
