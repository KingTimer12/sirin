class Animal {
    name: str
    mut count: u8

    default {
        count = 0
    }

    init(n: str) {
        name = n
        count = 0
    }

    fn describe() -> str => name
}

a: Animal = Animal("Rex")
b: Animal = Animal()
c: Animal = Animal { name: "Fido" }
