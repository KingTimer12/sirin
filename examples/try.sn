use sirin.io

fn parse(s: str) -> int! {
    if (s.len() == 0) {
        return Err("empty")
    }
    return Ok(s.to_int())
}

fn sum(a: str, b: str) -> int! {
    x ?= parse(a)
    y ?= parse(b)
    return Ok(x + y)
}

fn main() {
    if Ok(v) = sum("2", "3") {
        println(v)
    } else {
        println("error in sum")
    }

    if Err(e) = sum("", "3") {
        println(e)
    }

    r = parse::try("10")
    if Ok(v) = r {
        println(v)
    }
}
